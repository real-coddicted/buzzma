package com.coddicted.buzzma.claim.client;

/** Score API dataset keys, one per screenshot type, identifying which scoring config to apply. */
public interface ScoreDatasetKeys {
  String ORDER = "orderData";
  String RATING = "ratingData";
  String REVIEW = "reviewData";
  String RETURN = "returnData";
}
