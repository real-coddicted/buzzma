package com.coddicted.buzzma.configsdk.model;

/** Mirrors {@code ValueTypeEnum} on the Config API service. Enum names must stay identical. */
public enum ValueType {
  BOOLEAN,
  STRING,
  NUMBER,
  JSON
}
